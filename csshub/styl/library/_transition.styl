//
// Vendor "transitions" support.
//
transitions()
	vendor-prefixes('transitions', arguments, only: webkit moz o official)

//
// Vendor "transition-delay" support.
//
transition-delay()
	vendor-prefixes('transition-delay', arguments, only: webkit moz o official)

//
// Vendor "transition-duration" support.
//
transition-duration()
	vendor-prefixes('transition-duration', arguments, only: webkit moz o official)

//
// Vendor "transition-property" support.
//
transition-property()
	vendor-prefixes('transition-property', arguments, only: webkit moz o official)

//
// Vendor "transition-timing-function" support.
//
transition-timing-function()
	vendor-prefixes('transition-timing-function', arguments, only: webkit moz o official)