//
// Vendor "columns" support.
//
columns()
	vendor-prefixes('columns', arguments, only: webkit moz official)

//
// Vendor "column-count" support.
//
column-count()
	vendor-prefixes('column-count', arguments, only: webkit moz official)

//
// Vendor "column-gap" support.
//
column-gap()
	vendor-prefixes('column-gap', arguments, only: webkit moz official)

//
// Vendor "column-width" support.
//
column-width()
	vendor-prefixes('column-width', arguments, only: webkit moz official)

//
// Vendor "column-rule" support.
//
column-rule()
	vendor-prefixes('column-rule', arguments, only: webkit moz official)