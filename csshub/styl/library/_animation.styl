//
// Vendor "animation" support.
//
animation()
	vendor-prefixes('animation', arguments, only: webkit moz o official)

//
// Vendor "animation-delay" support.
//
animation-delay()
	vendor-prefixes('animation-delay', arguments, only: webkit moz o official)

//
// Vendor "animation-direction" support.
//
animation-direction()
	vendor-prefixes('animation-direction', arguments, only: webkit moz o official)

//
// Vendor "animation-duration" support.
//
animation-duration()
	vendor-prefixes('animation-duration', arguments, only: webkit moz o official)

//
// Vendor "animation-fill-mode" support.
//
animation-fill-mode()
	vendor-prefixes('animation-fill-mode', arguments, only: webkit moz o official)

//
// Vendor "animation-iteration-count" support.
//
animation-iteration-count()
	vendor-prefixes('animation-iteration-count', arguments, only: webkit moz o official)

//
// Vendor "animation-name" support.
//
animation-name()
	vendor-prefixes('animation-name', arguments, only: webkit moz o official)

//
// Vendor "animation-play-state" support.
//
animation-play-state()
	vendor-prefixes('animation-play-state', arguments, only: webkit moz o official)

//
// Vendor "animation-timing-function" support.
//
animation-timing-function()
	vendor-prefixes('animation-timing-function', arguments, only: webkit moz o official)